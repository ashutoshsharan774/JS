const descripter = Object.getOwnPropertyDescriptor(Math, "PI")

// console.log(descripter);

// console.log(Math.PI);
// Math.PI = 5
// console.log(Math.PI);
/*In JavaScript, Object.getOwnPropertyDescriptor() is a method that returns a property descriptor for a named property on an object. A property descriptor is an object that describes the attributes of a property, such as its value, whether it's writable, enumerable, and configurable.

Here's the syntax of Object.getOwnPropertyDescriptor():

javascript
Copy code
Object.getOwnPropertyDescriptor(obj, prop) */

const chai = {
    name: 'ginger chai',
    price: 250,
    isAvailable: true,

    orderChai: function(){
        console.log("chai nhi bni");
    }
}

console.log(Object.getOwnPropertyDescriptor(chai, "name"));

Object.defineProperty(chai, 'name', {
    //writable: false,
    enumerable: true,
    
})

console.log(Object.getOwnPropertyDescriptor(chai, "name"));

for (let [key, value] of Object.entries(chai)) {
    if (typeof value !== 'function') {
        
        console.log(`${key} : ${value}`);
    }
}